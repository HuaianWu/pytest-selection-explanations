现在用的测试套件比较大，平时经常把 `-k`、`-m`、`--deselect`、`--lf` 等选项组合起来用。有人发现一个测试没执行时，pytest 最后只告诉他 deselected 了多少条，很难判断到底是哪一层把它过滤掉的。

希望 pytest 增加一个可选的 selection explain 模式，在 collection 结束后可以看到指定测试或全部未运行测试的取消选择原因。内置筛选器应给出能读懂的原因，多个条件叠加时结果要稳定；同时提供适合 CI 留档的结构化输出。现有插件仍然可以继续通过 `pytest_deselected` 工作，对于无法判断来源的第三方筛选也不要编造原因。

默认输出和收集行为保持不变，不能为了生成报告重新收集或执行测试。请补齐命令行帮助、文档和 collection、mark、cache 以及插件兼容相关测试。
